`RAW_3_Level_ZZ_Semafor.mq5` is the original indicator without the added market structure lines feature.

`MOD_3_Level_ZZ_Semafor.mq5` is a modification of the original indicator, but with an inefficient implementation of the required feature.

Create a new indicator that uses DRAW_COLOR_SECTION buffers to implement the structure lines.
